# Agent Skills

Cross-client skills for this repository live under **`.agents/skills/<name>/SKILL.md`**.

Cursor may discover the same trees via `.cursor/skills/<name>` symlinks into `.agents/skills/`. Keep skill bodies only under `.agents/skills/` — do not duplicate them under `.cursor/skills/` or `.claude/skills/`.
